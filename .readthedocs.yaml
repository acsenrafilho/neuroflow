version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.10"
  commands:
    - pip install poetry
    - poetry install --no-interaction --with docs
    - poetry run mkdocs build --strict

mkdocs:
  configuration: mkdocs.yml

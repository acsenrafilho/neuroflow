## Summary

<!-- What does this PR change and why? Link the issue: Fixes #NNN -->

## Checklist

- [ ] `make lint` (pre-commit) and `make test` pass locally
- [ ] Frontend built if HTML/CSS/JS under `frontend/` changed (`make frontend-build`)
- [ ] User-visible docs updated (`docs/` and/or `frontend/src/pages/help/`) when behavior changed
- [ ] No PHI, identifiable scans, or real subject IDs in logs, screenshots, or fixtures
